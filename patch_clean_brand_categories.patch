--- a/app/(storefront)/[branch]/page.tsx
+++ b/app/(storefront)/[branch]/page.tsx
@@ -1,15 +1,51 @@
 
+'use client';
+import { useEffect, useMemo, useState } from 'react';
 import { getProductsByBranch } from '@/lib/api';
-import ProductGrid from '@/components/ProductGrid';
+import ProductListItem from '@/components/ProductListItem';
+import SearchBar from '@/components/SearchBar';
+import CategoryTabs from '@/components/CategoryTabs';
 
-export default async function BranchPage({ params }: { params: { branch: string } }) {
-  const products = await getProductsByBranch(params.branch);
+export default function BranchPage({ params }: { params: { branch: string } }) {
+  const [products, setProducts] = useState<any[]>([]);
+  const [q, setQ] = useState('');
+  const [active, setActive] = useState(0);
+
+  useEffect(() => {
+    (async () => {
+      const data = await getProductsByBranch(params.branch);
+      setProducts(data);
+    })();
+  }, [params.branch]);
+
+  const tabs = useMemo(() => {
+    const set = new Set<string>();
+    products.forEach(p => set.add(p.category || 'General'));
+    return Array.from(set);
+  }, [products]);
+
+  const filtered = useMemo(() => {
+    const list = !q ? products : products.filter(p => [p.name, p.description].join(' ').toLowerCase().includes(q.toLowerCase()));
+    if (tabs.length === 0) return list;
+    const cat = tabs[active] || tabs[0];
+    return list.filter(p => (p.category || 'General') === cat);
+  }, [q, products, tabs, active]);
+
   return (
-    <section className="container mx-auto px-4 py-6 space-y-6">
-      <div className="flex items-baseline justify-between">
-        <h1 className="text-2xl font-bold">Sucursal: {params.branch}</h1>
+    <section className="container mx-auto px-4 py-4 space-y-4">
+      <div className="rounded-2xl bg-white dark:bg-zinc-900 border p-3 shadow-soft">
+        <div className="flex items-center justify-between gap-3 mb-3">
+          <div className="font-semibold">Tienda: <span className="opacity-80">{params.branch}</span></div>
+        </div>
+        <SearchBar onSearch={(term) => setQ(term)} />
+        <div className="mt-3">
+          <CategoryTabs tabs={tabs} active={active} onChange={setActive} />
+        </div>
       </div>
-      <ProductGrid products={products} />
+
+      <div className="space-y-3 pb-24">
+        {filtered.map((p) => <ProductListItem key={p.id} product={p} />)}
+      </div>
     </section>
   );
 }
--- a/app/globals.css
+++ b/app/globals.css
@@ -13,3 +13,6 @@
 }
 
 a { text-decoration: none; }
+
+/* utilities */
+.line-clamp-2{display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;}--- a/app/layout.tsx
+++ b/app/layout.tsx
@@ -7,7 +7,7 @@
 
 const poppins = Poppins({ subsets: ['latin'], weight: ['400','600','800'] });
 
-export const metadata = { title: 'TracaBot Storefront', description: 'Catálogo y pedidos' };
+export const metadata = { title: 'Bot Storefront', description: 'Catálogo y pedidos' };
 
 export default function RootLayout({ children }: { children: ReactNode }) {
   return (
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -14,7 +14,7 @@
         href="/tienda-de-ventas"
         className="inline-flex items-center justify-center bg-primary text-primary-foreground rounded-2xl px-5 py-3 font-semibold"
       >
-        Ver catálogo de ejemplo de la tienda de ventas
+        Ver catálogo de ejemplo
       </Link>
     </main>
   );
--- a/components/BottomCheckoutBar.tsx
+++ b/components/BottomCheckoutBar.tsx
@@ -0,0 +1,25 @@
+
+'use client';
+import { useCart } from '@/store/cart';
+import { formatMoney } from '@/lib/currency';
+import { Button } from '@/components/ui/button';
+
+export default function BottomCheckoutBar() {
+  const { items, totalCents } = useCart();
+  const total = totalCents();
+  const qty = items.reduce((a, i) => a + i.qty, 0);
+  return (
+    <div className="fixed bottom-4 left-1/2 -translate-x-1/2 w-[calc(100%-24px)] sm:w-[560px]">
+      <div className="rounded-2xl bg-black text-white dark:bg-white dark:text-black flex items-center justify-between px-3 py-3 shadow-2xl">
+        <div className="flex items-center gap-3">
+          <div className="w-9 h-9 rounded-lg bg-white/20 dark:bg-black/20 flex items-center justify-center font-bold">{qty}</div>
+          <span className="font-semibold">Finalizar compra</span>
+        </div>
+        <div className="flex items-center gap-2">
+          <span className="font-semibold">{formatMoney(total, items[0]?.currency ?? 'USD')}</span>
+          <Button className="rounded-xl bg-white text-black dark:bg-black dark:text-white">Continuar</Button>
+        </div>
+      </div>
+    </div>
+  );
+}
--- a/components/CategoryTabs.tsx
+++ b/components/CategoryTabs.tsx
@@ -0,0 +1,22 @@
+
+'use client';
+import clsx from 'clsx';
+
+export default function CategoryTabs({ tabs, active = 0, onChange }: { tabs: string[]; active?: number; onChange: (i: number) => void }) {
+  return (
+    <div className="grid grid-cols-3 gap-2 text-sm">
+      {tabs.map((t, i) => (
+        <button
+          key={t}
+          onClick={() => onChange(i)}
+          className={clsx(
+            'rounded-xl px-3 py-2 border',
+            i === active ? 'bg-black text-white dark:bg-white dark:text-black' : 'bg-white dark:bg-zinc-900'
+          )}
+        >
+          {t}
+        </button>
+      ))}
+    </div>
+  );
+}
--- a/components/Header.tsx
+++ b/components/Header.tsx
@@ -1,8 +1,7 @@
 
 'use client';
 import Link from 'next/link';
-import ThemeToggle from '@/components/ThemeToggle';
-import BranchSelect from '@/components/BranchSelect';
+
 import CartDrawer from '@/components/CartDrawer';
 import { usePathname } from 'next/navigation';
 
@@ -14,12 +13,10 @@
     <header className="sticky top-0 z-50 backdrop-blur supports-[backdrop-filter]:bg-white/70 dark:supports-[backdrop-filter]:bg-zinc-950/70 bg-white/90 dark:bg-zinc-950/80 border-b">
       <div className="container mx-auto px-4 py-3 flex items-center gap-3">
         <Link href="/" className="font-extrabold text-xl tracking-tight">
-          <span className="text-primary">Traca</span>Bot <span className="text-secondary">Store</span>
+          Bot Store
         </Link>
         <div className="ml-auto flex items-center gap-3">
-          <BranchSelect current={currentBranch} />
           <CartDrawer />
-          <ThemeToggle />
         </div>
       </div>
     </header>
--- a/components/ProductListItem.tsx
+++ b/components/ProductListItem.tsx
@@ -0,0 +1,40 @@
+
+'use client';
+import Image from 'next/image';
+import { Button } from '@/components/ui/button';
+import { Product } from '@/lib/types';
+import { formatMoney } from '@/lib/currency';
+import { useCart } from '@/store/cart';
+
+export default function ProductListItem({ product }: { product: Product }) {
+  const add = useCart(s => s.add);
+  return (
+    <div className="flex gap-3 p-3 rounded-2xl bg-white dark:bg-zinc-900 border shadow-soft">
+      {product.imageUrl && (
+        <div className="relative w-[110px] h-[110px] shrink-0 overflow-hidden rounded-xl">
+          <Image src={product.imageUrl} alt={product.name} fill className="object-cover" />
+        </div>
+      )}
+      <div className="flex-1 min-w-0">
+        <h3 className="text-base font-semibold truncate">{product.name}</h3>
+        {product.description && <p className="text-sm opacity-80 line-clamp-2">{product.description}</p>}
+        <div className="mt-2 font-semibold">{formatMoney(product.priceCents, product.currency)}</div>
+      </div>
+      <div className="flex items-center">
+        <button
+          onClick={() =>
+            add({
+              productId: product.id,
+              name: product.name,
+              qty: 1,
+              unitPriceCents: product.priceCents,
+              currency: product.currency,
+            })
+          }
+          className="w-11 h-11 rounded-full bg-white dark:bg-zinc-800 border shadow-soft flex items-center justify-center text-xl"
+          title="Agregar"
+        >+</button>
+      </div>
+    </div>
+  );
+}
--- a/components/SearchBar.tsx
+++ b/components/SearchBar.tsx
@@ -0,0 +1,22 @@
+
+'use client';
+import { useState } from 'react';
+import { Button } from '@/components/ui/button';
+
+export default function SearchBar({ onSearch }: { onSearch: (q: string) => void }) {
+  const [q, setQ] = useState('');
+  return (
+    <div className="flex items-center gap-2">
+      <input
+        value={q}
+        onChange={(e) => setQ(e.target.value)}
+        placeholder="Buscar producto..."
+        className="flex-1 rounded-xl border px-3 py-2 bg-white dark:bg-zinc-900"
+      />
+      {q && (
+        <Button className="rounded-xl" onClick={() => { setQ(''); onSearch(''); }} title="Limpiar">✕</Button>
+      )}
+      <Button className="rounded-xl" onClick={() => onSearch(q)} title="Buscar">🔍</Button>
+    </div>
+  );
+}
--- a/lib/types.ts
+++ b/lib/types.ts
@@ -10,6 +10,7 @@
   currency: Currency;
   imageUrl?: string;
   isActive: boolean;
+  category?: string;
 }
 
 export interface CartItem {
--- a/mocks/products.json
+++ b/mocks/products.json
@@ -1,35 +1,98 @@
 [
   {
-    "id": "p1",
+    "id": "promo1",
+    "branchId": "tienda-de-ventas",
+    "name": "Triple Pizza (3 medianas + Pepsi)",
+    "slug": "promo-triple-pizza",
+    "description": "Promoci\u00f3n destacada",
+    "priceCents": 1199,
+    "currency": "USD",
+    "imageUrl": "/images/promo-triple-pizza.webp",
+    "isActive": true,
+    "category": "Promociones"
+  },
+  {
+    "id": "promo2",
+    "branchId": "tienda-de-ventas",
+    "name": "Mediana Agrandada XL (4 ingredientes)",
+    "slug": "promo-mediana-agrandada-xl-1",
+    "description": "Promoci\u00f3n destacada",
+    "priceCents": 499,
+    "currency": "USD",
+    "imageUrl": "/images/promo-mediana-agrandada-xl-1.webp",
+    "isActive": true,
+    "category": "Promociones"
+  },
+  {
+    "id": "promo3",
+    "branchId": "tienda-de-ventas",
+    "name": "Combo Buenazooo!! (4 medianas + 2 Pepsi + pan de ajo)",
+    "slug": "promo-buenazooo-4medianas",
+    "description": "Promoci\u00f3n destacada",
+    "priceCents": 1999,
+    "currency": "USD",
+    "imageUrl": "/images/promo-buenazooo-4medianas.webp",
+    "isActive": true,
+    "category": "Promociones"
+  },
+  {
+    "id": "promo4",
+    "branchId": "tienda-de-ventas",
+    "name": "Mediana Agrandada XL (varios sabores)",
+    "slug": "promo-mediana-agrandada-xl-2",
+    "description": "Promoci\u00f3n destacada",
+    "priceCents": 1999,
+    "currency": "USD",
+    "imageUrl": "/images/promo-mediana-agrandada-xl-2.webp",
+    "isActive": true,
+    "category": "Promociones"
+  },
+  {
+    "id": "promo5",
+    "branchId": "tienda-de-ventas",
+    "name": "2 Medianas XL (32cm) \u2013 4 ingredientes",
+    "slug": "promo-2x-mediana-xl",
+    "description": "Promoci\u00f3n destacada",
+    "priceCents": 999,
+    "currency": "USD",
+    "imageUrl": "/images/promo-2x-mediana-xl.webp",
+    "isActive": true,
+    "category": "Promociones"
+  },
+  {
+    "id": "promo6",
+    "branchId": "tienda-de-ventas",
+    "name": "Mediana Especial (25cm) + Pepsi 1L",
+    "slug": "promo-mediana-especial",
+    "description": "Promoci\u00f3n destacada",
+    "priceCents": 799,
+    "currency": "USD",
+    "imageUrl": "/images/promo-mediana-especial.webp",
+    "isActive": true,
+    "category": "Promociones"
+  },
+  {
+    "id": "pz1",
     "branchId": "tienda-de-ventas",
     "name": "Pizza Margarita",
     "slug": "pizza-margarita",
-    "description": "Tomate, mozzarella, albahaca",
+    "description": "Tomate, mozzarella, albahaca fresca",
     "priceCents": 899,
     "currency": "USD",
     "imageUrl": "/images/margarita.png",
-    "isActive": true
+    "isActive": true,
+    "category": "Pizzas"
   },
   {
-    "id": "p2",
+    "id": "pz2",
     "branchId": "tienda-de-ventas",
     "name": "Pepperoni",
     "slug": "pepperoni",
-    "description": "Queso y pepperoni",
+    "description": "Queso y pepperoni crujiente",
     "priceCents": 1099,
     "currency": "USD",
     "imageUrl": "/images/pepperoni.png",
-    "isActive": true
-  },
-  {
-    "id": "p3",
-    "branchId": "tienda-de-ventas",
-    "name": "Hawaiana",
-    "slug": "hawaiana",
-    "description": "Jam\u00f3n, pi\u00f1a y queso",
-    "priceCents": 1199,
-    "currency": "USD",
-    "imageUrl": "/images/hawaiana.png",
-    "isActive": true
+    "isActive": true,
+    "category": "Pizzas"
   }
 ]